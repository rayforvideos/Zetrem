// What --model accepts. The CLI decides these names, so they are read and
// written here rather than in whatever happens to choose between them.
export type ModelChoice = 'default' | 'fable' | 'opus' | 'sonnet' | 'haiku'
