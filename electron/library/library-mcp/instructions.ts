// What the agent is told about the library when it connects. Held here, in the
// app, so a project never carries a file that shapes how agents behave.
export const INSTRUCTIONS = `# 라이브러리

이 프로젝트의 라이브러리다. 이전 세션이 남긴 결론과 결정이 여기 있다.
작업하며 알게 된 것은 여기에 남긴다. 이 폴더는 프로젝트와 함께 움직인다.

## 쓰는 법

- 사람이나 이전 세션이 여기 적어 둔 것이면 무엇이든 답이 될 수 있다: 프로젝트의 결정과
  이력뿐 아니라 사람이 알려 준 이름, 취향, 약속 같은 것도.
- "모른다", "기록이 없다", "저장돼 있지 않다"고 답하기 전에는 반드시 \`library_search\`로
  먼저 찾아본다. 찾아보지 않고 모른다고 하지 않는다.
- 건너뛰어도 되는 건 인사, 일반 상식, 눈앞의 코드나 대화만으로 답이 나오는 것뿐이다.
- 한 노트에 한 결론. 첫 문단이 요약이 되도록 결론부터 쓴다.
- 새 노트는 \`library_write\` 도구로 쓴다. 출처와 세션이 자동으로 기록된다.
- 관련 노트는 [[노트 제목]]으로 잇는다. 이미 있는 노트를 고칠 때는 덮어쓰지 말고 덧붙인다.

## 파일 모양

노트는 마크다운 파일이다. 머리에 제목, 만든 때, 고친 때, 태그가 있고
그 아래가 본문이다. 폴더는 있어도 되고 없어도 된다.
`
