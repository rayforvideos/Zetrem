// What the operating system says about this app's notifications: allowed,
// switched off by the user, or never decided (macOS asks on the first one).
export type NotifyState = 'allowed' | 'denied' | 'unasked'
